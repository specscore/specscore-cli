# Reusable checks

Shared verification units (`<slug>.check.md`) invoked from the sibling suites via `**Use:**` — Feature: [cli/rehearse/reusable-checks](../../../reusable-checks/README.md). Not scenarios; never run directly.

## Open Questions

None at this time.
