```yaml
feature: cli/self-update
revision: e766e7e
verify_revision: 35ef702
drift:
  - ac: cli/self-update#ac:canonical-and-alias
    verdict: no-drift
    narrative: "Single cobra.Command (Use:self-update, Aliases:[update]) wired once into root.go; both names share one RunE and identical --check dispatch."
  - ac: cli/self-update#ac:check-is-readonly
    verdict: no-drift
    narrative: "runCheck routes before the install-method switch for all methods; only resolveLatest + Compare (read-only), never reaches Downloader/ReplaceExecutable."
  - ac: cli/self-update#ac:confirm-prompt-and-yes
    verdict: no-drift
    narrative: "Manual path prints current → latest; interactive without --yes prompts Proceed? [y/N] and proceeds only on y/yes; --yes skips prompt and calls doSelfReplace."
  - ac: cli/self-update#ac:noninteractive-without-yes-refuses
    verdict: no-drift
    narrative: "Non-interactive + no --yes prints '--yes is required' to stderr and returns InvalidStateError (exit 4) before doSelfReplace; AC requires only non-zero, which 4 satisfies."
  - ac: cli/self-update#ac:managed-is-redirected
    verdict: no-drift
    narrative: "Managed branch prints ManagerName + exact UpgradeCommand (brew/scoop/winget) then returns nil (exit 0) with no download or write."
  - ac: cli/self-update#ac:manual-is-eligible
    verdict: no-drift
    narrative: "Classify maps /usr/local/bin/specscore and go/bin/specscore to Manual/eligible, matching the AC's two examples; managed managers excluded first."
  - ac: cli/self-update#ac:ambiguous-falls-back-safe
    verdict: no-drift
    narrative: "Ambiguous branch prints ambiguousGuidance (ambiguity + manual steps), never downloads/replaces, exits non-zero via InvalidStateError (4); spec requires only non-zero."
  - ac: cli/self-update#ac:latest-stable-only
    verdict: code-tighter-than-spec
    narrative: "AC names skipping prereleases only; LatestStableTag also skips drafts (rel.Prerelease || rel.Draft) — a broader exclusion the AC does not name, though consistent with intent."
  - ac: cli/self-update#ac:dev-build-is-undetermined
    verdict: no-drift
    narrative: "dev build → Compare returns Undetermined; runCheck prints 'version is undetermined' (not up to date) and returns exit 10 via Verdict.ExitCode()."
  - ac: cli/self-update#ac:checksum-mismatch-aborts
    verdict: no-drift
    narrative: "doSelfReplace verifies sha256 vs checksums.txt before extraction; mismatch/missing returns non-zero exitcode.Error with empty path so ReplaceExecutable is never reached."
  - ac: cli/self-update#ac:replace-is-atomic
    verdict: no-drift
    narrative: "replace.go implements atomic swap for all three named OSes (POSIX os.Rename; Windows move-aside); staging failure leaves original intact. Caveat: only POSIX path tested (CI is ubuntu-only) — a coverage gap, not spec/code drift."
  - ac: cli/self-update#ac:already-current-noop
    verdict: no-drift
    narrative: "Manual non-check path short-circuits on Compare==UpToDate: prints already-up-to-date and returns nil (exit 0) before any download/replace."
  - ac: cli/self-update#ac:check-exit-code-contract
    verdict: no-drift
    narrative: "runCheck returns 0 (UpToDate), 10 (Available via Verdict.ExitCode), 3 (NotFound) on lookup error — three distinct codes; the generic Unexpected=10 is not on the --check lookup-error path."
  - ac: cli/self-update#ac:network-failure-is-safe
    verdict: no-drift
    narrative: "Lookup and download failures print a clear release/download error and return non-zero *exitcode.Error; ReplaceExecutable runs only after a successful download, so the binary is never touched."
  - ac: cli/self-update#ac:permission-denied-is-safe
    verdict: no-drift
    narrative: "classifySelfReplaceError matches fs.ErrPermission, prints the executable path + sudo/package-manager remedy, returns non-zero; swap never succeeds so the original binary stays intact."
```

# Recap Report — cli/self-update @ e766e7e

**Feature:** [cli/self-update](../README.md)
**Run revision:** e766e7e
**Verify revision:** 35ef702 ([_verify/35ef702.md](../_verify/35ef702.md))
**Drift summary:** 14 no-drift, 0 spec-tighter, 1 code-tighter, 0 contradiction, 0 unmapped, 0 errored.

Exit code 0 — no contradictions, no errors. One informational `code-tighter-than-spec` drift (latest-stable-only) is recorded below for `review`/`ship` to weigh; it does not block at the recap layer.

## AC: canonical-and-alias

**Drift verdict:** no-drift

**Narrative:** Single `cobra.Command` (`Use:"self-update"`, `Aliases:["update"]`) wired once into root.go; both names share one `RunE` and an identical `--check` dispatch via `runCheck`, so the two invocations produce identical output/exit.

**Verify verdict:** pass

**Verify justification:** Single cobra.Command with Aliases:[update]; self-update and update share RunE and --check dispatch identically. Test passes.

**Commits:**
- a0d876a — feat(cli/self-update): register self-update command with update alias

**Evidence:**
- internal/cli/self_update.go (Aliases:["update"], shared RunE→runCheck)
- internal/cli/root.go (single AddCommand(selfUpdateCommand()))

## AC: check-is-readonly

**Drift verdict:** no-drift

**Narrative:** `runCheck` routes before the install-method switch for all methods; it only calls `resolveLatest` + `selfupdate.Compare` (pure read) and never reaches `doSelfReplace`/`Downloader`/`ReplaceExecutable`, so the on-disk binary is untouched.

**Verify verdict:** pass

**Verify justification:** runCheck routes before the install-method switch for all methods, only resolves latest + Compare (read-only), prints availability + next step, never reaches doSelfReplace/Downloader/write.

**Commits:**
- 4f08494 — feat(cli/self-update): --check read-only mode with exit-code contract

**Evidence:**
- internal/cli/self_update.go (--check intercept before method switch; runCheck read-only)

## AC: confirm-prompt-and-yes

**Drift verdict:** no-drift

**Narrative:** Manual path prints `<current> → <latest>`; interactive without `--yes` prompts `Proceed? [y/N]` and proceeds only on y/yes; `--yes` skips the prompt and calls `doSelfReplace` directly — matches the AC exactly.

**Verify verdict:** pass

**Verify justification:** Manual path prints current → latest; with --yes calls doSelfReplace without prompting; interactive without --yes prompts and proceeds only on y/yes. Tests pass.

**Commits:**
- 85a82ec — feat(cli/self-update): confirmation prompt, --yes, non-interactive guard

**Evidence:**
- internal/cli/self_update.go (transition print, --yes guard, interactive prompt)

## AC: noninteractive-without-yes-refuses

**Drift verdict:** no-drift

**Narrative:** Non-interactive run without `--yes` prints `--yes is required for non-interactive use` to stderr and returns `InvalidStateError` (exit 4) before `doSelfReplace`, leaving the binary untouched. The AC requires only "exits non-zero", which exit 4 satisfies.

**Verify verdict:** pass

**Verify justification:** Non-interactive + no --yes prints "--yes is required for non-interactive use", returns InvalidStateError (exit 4) before doSelfReplace; binary unchanged. Test passes.

**Commits:**
- 85a82ec — feat(cli/self-update): confirmation prompt, --yes, non-interactive guard

**Evidence:**
- internal/cli/self_update.go (non-interactive guard)
- pkg/exitcode/exitcode.go (InvalidState = 4, non-zero)

## AC: managed-is-redirected

**Drift verdict:** no-drift

**Narrative:** Managed branch prints `ManagerName` + exact `UpgradeCommand` (`brew upgrade specscore` / `scoop update specscore` / `winget upgrade SpecScore.CLI`) then returns nil (exit 0) with no download or filesystem write.

**Verify verdict:** pass

**Verify justification:** Managed branch prints ManagerName + exact UpgradeCommand (brew upgrade/scoop update/winget upgrade) then returns nil (exit 0) with no download/write. Tests pass.

**Commits:**
- 8c89214 — feat(cli/self-update): managed redirect dispatch + asset download/verify

**Evidence:**
- internal/cli/self_update.go (Managed case)
- internal/selfupdate/redirect.go (UpgradeCommand + ManagerName)

## AC: manual-is-eligible

**Drift verdict:** no-drift

**Narrative:** `Classify` maps `/usr/local/bin/specscore` (release archive) and `/home/u/go/bin/specscore` (go install) to Manual/eligible, matching the AC's two examples; the broader `*/bin/` heuristic still honors the spec's intent and managed managers are matched first.

**Verify verdict:** pass

**Verify justification:** Classify maps /usr/local/bin/specscore and go/bin/specscore to Manual (self-replace eligible), never Ambiguous/Managed. Tests pass.

**Commits:**
- f9f93ad — feat(selfupdate): latest-stable release resolution and install detection

**Evidence:**
- internal/selfupdate/detect.go (/go/bin/ + /bin suffix rules → Manual)

## AC: ambiguous-falls-back-safe

**Drift verdict:** no-drift

**Narrative:** Ambiguous branch prints `ambiguousGuidance` (states ambiguity + manual-update steps), never downloads/replaces (returns before any write), and exits non-zero via `InvalidStateError` (4); the spec requires only "non-zero".

**Verify verdict:** pass

**Verify justification:** Ambiguous branch prints ambiguousGuidance and returns InvalidStateError (exit 4) before any download/replace. Test passes.

**Commits:**
- aeaa992 — feat(cli/self-update): ambiguous-fallback + atomic executable replace

**Evidence:**
- internal/cli/self_update.go (Ambiguous case; ambiguousGuidance const)

## AC: latest-stable-only

**Drift verdict:** code-tighter-than-spec

**Narrative:** The AC requires "latest" to skip prereleases only; `LatestStableTag` also skips drafts (`rel.Prerelease || rel.Draft`), a filter the AC does not name. This is a broader exclusion than specified — consistent with the AC's intent (a draft is not a public release), but stricter than the literal text. Informational; not blocking at recap.

**Verify verdict:** pass

**Verify justification:** LatestStableTag skips prerelease/draft and returns the newest stable; test serves newest-prerelease + older-stable and asserts the stable tag.

**Commits:**
- f9f93ad — feat(selfupdate): latest-stable release resolution and install detection

**Evidence:**
- internal/selfupdate/release.go (`if rel.Prerelease || rel.Draft { continue }`)
- internal/selfupdate/release_test.go (TestLatestStableTag_SkipsDraft — draft-skipping beyond the AC)

## AC: dev-build-is-undetermined

**Drift verdict:** no-drift

**Narrative:** A `dev` build → `Compare` returns `Undetermined`; `runCheck` prints that the version is undetermined (not "up to date") and returns exit 10 via `Verdict.ExitCode()` — matching the AC exactly.

**Verify verdict:** pass

**Verify justification:** Compare('dev',...) returns Undetermined; runCheck prints 'undetermined' (not up to date) and returns exit 10 via Verdict.ExitCode. Tests pass.

**Commits:**
- f9f93ad — feat(selfupdate): latest-stable release resolution and install detection

**Evidence:**
- internal/selfupdate/release.go (Compare dev→Undetermined; Verdict.ExitCode)
- internal/cli/self_update.go (runCheck Undetermined case)

## AC: checksum-mismatch-aborts

**Drift verdict:** no-drift

**Narrative:** `doSelfReplace` verifies sha256 against `checksums.txt` before any extraction; a mismatch or missing entry returns a non-zero `exitcode.Error` with an empty path so `ReplaceExecutable` is never reached and the running binary is untouched.

**Verify verdict:** pass

**Verify justification:** DownloadAndVerify computes sha256 and compares to checksums.txt before extraction; mismatch/missing returns non-zero exitcode.Error with empty path, never touching the running binary. Tests pass.

**Commits:**
- 8c89214 — feat(cli/self-update): managed redirect dispatch + asset download/verify

**Evidence:**
- internal/selfupdate/download.go (verify-before-extract)
- internal/cli/self_update.go (doSelfReplace gates ReplaceExecutable behind DownloadAndVerify)

## AC: replace-is-atomic

**Drift verdict:** no-drift

**Narrative:** `replace.go` implements atomic swap for all three named OSes (POSIX `os.Rename`; Windows move-aside) and a staging failure leaves the original intact, so code breadth matches the spec. Caveat: only the POSIX path is tested and CI runs ubuntu-only — the Windows branch and macOS are unverified, but that is a test-coverage gap, not spec/code drift in what the code delivers.

**Verify verdict:** pass

**Verify justification:** stage() copies to a temp file in the target dir then os.Rename atomically (Windows move-aside); staging failure leaves the original intact — test asserts complete original bytes, no truncation.

**Commits:**
- aeaa992 — feat(cli/self-update): ambiguous-fallback + atomic executable replace

**Evidence:**
- internal/selfupdate/replace.go (POSIX rename + Windows move-aside)
- internal/selfupdate/replace_test.go (atomic-on-staging-failure)
- .github/workflows (CI runs ubuntu-latest only — Windows/macOS untested)

## AC: already-current-noop

**Drift verdict:** no-drift

**Narrative:** Manual non-check path short-circuits on `Compare==UpToDate`: prints "already up to date" and returns nil (exit 0) before any `DownloadAndVerify`/`ReplaceExecutable`.

**Verify verdict:** pass

**Verify justification:** Manual non-check path: when Compare==UpToDate, prints already-up-to-date and returns nil (exit 0) before any download/replace. Test passes.

**Commits:**
- faf2dae — feat(cli/self-update): up-to-date no-op short-circuit on manual path

**Evidence:**
- internal/cli/self_update.go (Manual UpToDate short-circuit)

## AC: check-exit-code-contract

**Drift verdict:** no-drift

**Narrative:** `runCheck` returns 0 (UpToDate→nil), 10 (Available/Undetermined via `Verdict.ExitCode`), and 3 (`NotFound`) on a release-lookup error — three distinct codes exactly matching the AC; the generic `Unexpected=10` constant is unrelated and not on the `--check` lookup-error path.

**Verify verdict:** pass

**Verify justification:** runCheck returns 0 (UpToDate), 10 (Available via Verdict.ExitCode), and 3 (NotFound) for release-lookup error — all distinct. Test asserts 0/10/3.

**Commits:**
- 4f08494 — feat(cli/self-update): --check read-only mode with exit-code contract

**Evidence:**
- internal/cli/self_update.go (runCheck exit-code mapping)
- pkg/exitcode/exitcode.go (NotFound=3; Unexpected=10 unrelated)

## AC: network-failure-is-safe

**Drift verdict:** no-drift

**Narrative:** Both unreachable-source modes (`resolveLatest` lookup failure and `doSelfReplace` download failure) print a clear release/download error and return a non-zero `*exitcode.Error`; `ReplaceExecutable` runs only after a successful download, so the binary is never touched.

**Verify verdict:** pass

**Verify justification:** Release-lookup/download failures print a clear error and return a non-zero exitcode.Error before the swap (ReplaceExecutable runs only post-download); binary untouched. Test passes.

**Commits:**
- 35ef702 — feat(cli/self-update): network and permission failure handling; plan complete

**Evidence:**
- internal/cli/self_update.go (resolveLatest→NotFoundErrorf; classifySelfReplaceError for download errors)

## AC: permission-denied-is-safe

**Drift verdict:** no-drift

**Narrative:** `classifySelfReplaceError` matches `fs.ErrPermission`, prints the `os.Executable()` path plus a sudo/package-manager remedy, and returns a non-zero `*exitcode.Error`; the swap never succeeds so the original binary stays intact.

**Verify verdict:** pass

**Verify justification:** classifySelfReplaceError matches fs.ErrPermission, prints the executable path + sudo/package-manager remedy, returns non-zero; original binary untouched. Test passes.

**Commits:**
- 35ef702 — feat(cli/self-update): network and permission failure handling; plan complete

**Evidence:**
- internal/cli/self_update.go (classifySelfReplaceError permission branch)
