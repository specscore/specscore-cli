```yaml
feature: cli/self-update
revision: 35ef702
verdicts:
  - ac: cli/self-update#ac:canonical-and-alias
    verdict: pass
    justification: "Single cobra.Command with Aliases:[update]; self-update and update share RunE and --check dispatch identically. Test passes."
  - ac: cli/self-update#ac:check-is-readonly
    verdict: pass
    justification: "runCheck routes before the install-method switch for all methods, only resolves latest + Compare (read-only), prints availability + next step, never reaches doSelfReplace/Downloader/write."
  - ac: cli/self-update#ac:confirm-prompt-and-yes
    verdict: pass
    justification: "Manual path prints current → latest; with --yes calls doSelfReplace without prompting; interactive without --yes prompts and proceeds only on y/yes. Tests pass."
  - ac: cli/self-update#ac:noninteractive-without-yes-refuses
    verdict: pass
    justification: "Non-interactive + no --yes prints '--yes is required for non-interactive use', returns InvalidStateError (exit 4) before doSelfReplace; binary unchanged. Test passes."
  - ac: cli/self-update#ac:managed-is-redirected
    verdict: pass
    justification: "Managed branch prints ManagerName + exact UpgradeCommand (brew upgrade/scoop update/winget upgrade) then returns nil (exit 0) with no download/write. Tests pass."
  - ac: cli/self-update#ac:manual-is-eligible
    verdict: pass
    justification: "Classify maps /usr/local/bin/specscore and go/bin/specscore to Manual (self-replace eligible), never Ambiguous/Managed. Tests pass."
  - ac: cli/self-update#ac:ambiguous-falls-back-safe
    verdict: pass
    justification: "Ambiguous branch prints ambiguousGuidance (states ambiguous + manual guidance) and returns InvalidStateError (exit 4) before any download/replace. Test passes."
  - ac: cli/self-update#ac:latest-stable-only
    verdict: pass
    justification: "LatestStableTag skips prerelease/draft and returns the newest stable; test serves newest-prerelease + older-stable and asserts the stable tag."
  - ac: cli/self-update#ac:dev-build-is-undetermined
    verdict: pass
    justification: "Compare('dev',...) returns Undetermined; runCheck prints 'undetermined' (not up to date) and returns exit 10 via Verdict.ExitCode. Tests pass."
  - ac: cli/self-update#ac:checksum-mismatch-aborts
    verdict: pass
    justification: "DownloadAndVerify computes sha256 and compares to checksums.txt before extraction; mismatch/missing returns non-zero exitcode.Error with empty path, never touching the running binary. Tests pass."
  - ac: cli/self-update#ac:replace-is-atomic
    verdict: pass
    justification: "stage() copies to a temp file in the target dir then os.Rename atomically (Windows move-aside); staging failure leaves the original intact — test asserts complete original bytes, no truncation."
  - ac: cli/self-update#ac:already-current-noop
    verdict: pass
    justification: "Manual non-check path: when Compare==UpToDate, prints already-up-to-date and returns nil (exit 0) before any download/replace. Test passes."
  - ac: cli/self-update#ac:check-exit-code-contract
    verdict: pass
    justification: "runCheck returns 0 (UpToDate), 10 (Available via Verdict.ExitCode), and 3 (NotFound) for release-lookup error — all distinct. Test asserts 0/10/3."
  - ac: cli/self-update#ac:network-failure-is-safe
    verdict: pass
    justification: "Release-lookup/download failures print a clear error and return a non-zero exitcode.Error before the swap (ReplaceExecutable runs only post-download); binary untouched. Test passes."
  - ac: cli/self-update#ac:permission-denied-is-safe
    verdict: pass
    justification: "classifySelfReplaceError matches fs.ErrPermission, prints the executable path + sudo/package-manager remedy, returns non-zero; original binary untouched. Test passes."
```

# Verify Report — cli/self-update @ 35ef702

**Feature:** [cli/self-update](../README.md)
**Run revision:** 35ef702
**Summary:** 15 passed, 0 failed, 0 unmapped, 0 errored.

All 15 acceptance criteria are mapped to `Verifies:`-trailed commits and verified `pass` by independent per-AC verifier subagents. Exit code 0.

## AC: canonical-and-alias

**Verdict:** pass

**Justification:** A single `cobra.Command` declares `Aliases: []string{"update"}`, so `self-update` and `update` resolve to the same `RunE`, and `--check` dispatches to `runCheck` identically; the alias-equivalence test passes.

**Commits:**
- a0d876a — feat(cli/self-update): register self-update command with update alias

**Evidence:**
- internal/cli/self_update.go (Use:"self-update", Aliases:["update"], --check→runCheck)
- internal/cli/root.go (selfUpdateCommand registered)
- internal/cli/self_update_test.go TestSelfUpdate_CanonicalAndAlias

## AC: check-is-readonly

**Verdict:** pass

**Justification:** `runCheck` is dispatched before the install-method switch for every method; it only calls `resolveLatest` + `selfupdate.Compare` (read-only), prints availability and a method-appropriate next step, and never reaches `doSelfReplace`, `Downloader`, or any filesystem write.

**Commits:**
- 4f08494 — feat(cli/self-update): --check read-only mode with exit-code contract

**Evidence:**
- internal/cli/self_update.go (runCheck + early --check dispatch in RunE)
- internal/cli/self_update_test.go TestSelfUpdate_CheckIsReadonly

## AC: confirm-prompt-and-yes

**Verdict:** pass

**Justification:** The manual path prints the `current → latest` transition; with `--yes` it skips prompting and calls `doSelfReplace`; interactive without `--yes` prompts `Proceed? [y/N]` and proceeds only on y/yes. All three behaviors are covered by passing tests.

**Commits:**
- 85a82ec — feat(cli/self-update): confirmation prompt, --yes, non-interactive guard

**Evidence:**
- internal/cli/self_update.go (Manual case: transition print, --yes guard, interactive prompt, doSelfReplace call)
- internal/cli/self_update_test.go TestSelfUpdate_ConfirmPromptAndYes_WithYes / _InteractiveConfirms

## AC: noninteractive-without-yes-refuses

**Verdict:** pass

**Justification:** When `isInteractive()==false` and no `--yes`, the command prints `--yes is required for non-interactive use` to stderr, returns `InvalidStateError` (exit 4), and returns before `doSelfReplace`, leaving the binary unchanged; the test asserts the replace spy was not called.

**Commits:**
- 85a82ec — feat(cli/self-update): confirmation prompt, --yes, non-interactive guard

**Evidence:**
- internal/cli/self_update.go (non-interactive guard)
- internal/cli/self_update_test.go TestSelfUpdate_NonInteractiveWithoutYesRefuses

## AC: managed-is-redirected

**Verdict:** pass

**Justification:** The Managed branch prints `ManagerName` plus the exact `UpgradeCommand` (`brew upgrade specscore` / `scoop update specscore` / `winget upgrade SpecScore.CLI`) and returns nil (exit 0) before any download or write; tests pass for all three managers.

**Commits:**
- 8c89214 — feat(cli/self-update): managed redirect dispatch + asset download/verify

**Evidence:**
- internal/cli/self_update.go (Managed case)
- internal/selfupdate/redirect.go UpgradeCommand
- internal/cli/self_update_test.go TestSelfUpdate_ManagedIsRedirected; TestUpgradeCommand

## AC: manual-is-eligible

**Verdict:** pass

**Justification:** `Classify` maps `/usr/local/bin/specscore` and `/home/u/go/bin/specscore` to `{Method: Manual}` (neither Ambiguous nor Managed); the Manual classification is the self-replace-eligible result. Tests pass.

**Commits:**
- f9f93ad — feat(selfupdate): latest-stable release resolution and install detection

**Evidence:**
- internal/selfupdate/detect.go (/go/bin/ and /bin suffix rules → Manual)
- internal/selfupdate/detect_test.go TestClassify (manual cases)

## AC: ambiguous-falls-back-safe

**Verdict:** pass

**Justification:** The Ambiguous branch prints `ambiguousGuidance` (states the install method is ambiguous plus GitHub-releases / package-manager guidance) and returns `InvalidStateError` (exit 4) before any download/replace. Test passes.

**Commits:**
- aeaa992 — feat(cli/self-update): ambiguous-fallback + atomic executable replace

**Evidence:**
- internal/cli/self_update.go (Ambiguous case)
- internal/cli/self_update_test.go TestSelfUpdate_AmbiguousFallsBackSafe

## AC: latest-stable-only

**Verdict:** pass

**Justification:** `LatestStableTag` skips entries where `prerelease` or `draft` is true and returns the newest stable; a test serves a newest-prerelease + older-stable fixture and asserts the stable tag is selected.

**Commits:**
- f9f93ad — feat(selfupdate): latest-stable release resolution and install detection

**Evidence:**
- internal/selfupdate/release.go (LatestStableTag prerelease/draft skip)
- internal/selfupdate/release_test.go TestLatestStableTag_SkipsPrerelease

## AC: dev-build-is-undetermined

**Verdict:** pass

**Justification:** `Compare("dev", ...)` returns `Undetermined`; `runCheck` prints that the current version is undetermined (not "up to date") and returns exit 10 via `Verdict.ExitCode()`. Tests pass.

**Commits:**
- f9f93ad — feat(selfupdate): latest-stable release resolution and install detection
- 4f08494 — feat(cli/self-update): --check read-only mode with exit-code contract

**Evidence:**
- internal/selfupdate/release.go (Compare dev→Undetermined; Verdict.ExitCode)
- internal/cli/self_update.go (runCheck Undetermined case)
- internal/selfupdate/release_test.go TestCompare_DevIsUndetermined

## AC: checksum-mismatch-aborts

**Verdict:** pass

**Justification:** `DownloadAndVerify` computes the asset sha256 and compares to `checksums.txt` before extraction; a mismatch or missing entry returns a non-zero `exitcode.Error` with an empty path and never writes the running executable. Tests pass.

**Commits:**
- 8c89214 — feat(cli/self-update): managed redirect dispatch + asset download/verify

**Evidence:**
- internal/selfupdate/download.go (sha256 check before extractBinary)
- internal/selfupdate/download_test.go TestDownloadAndVerify_ChecksumMismatchAborts / _MissingChecksumEntry

## AC: replace-is-atomic

**Verdict:** pass

**Justification:** The new bytes are staged to a temp file in the target directory then `os.Rename`d over the target (atomic on POSIX; Windows move-aside for a running exe); a staging failure leaves the original intact — the test asserts the complete original bytes remain, never truncated.

**Commits:**
- aeaa992 — feat(cli/self-update): ambiguous-fallback + atomic executable replace

**Evidence:**
- internal/selfupdate/replace.go (stage + atomic os.Rename, Windows .old move-aside)
- internal/selfupdate/replace_test.go TestReplaceExecutableAtomicOnStagingFailure

## AC: already-current-noop

**Verdict:** pass

**Justification:** On the manual non-check path, when `Compare == UpToDate`, the command prints an up-to-date message and returns nil (exit 0) before any download or replace. Test passes.

**Commits:**
- faf2dae — feat(cli/self-update): up-to-date no-op short-circuit on manual path

**Evidence:**
- internal/cli/self_update.go (Manual UpToDate short-circuit)
- internal/cli/self_update_test.go TestSelfUpdate_AlreadyCurrentNoop

## AC: check-exit-code-contract

**Verdict:** pass

**Justification:** `runCheck` returns 0 (UpToDate), 10 (Available, via `Verdict.ExitCode`), and 3 (`NotFound`) for a release-lookup error — three distinct codes; the test asserts 0/10/3.

**Commits:**
- 4f08494 — feat(cli/self-update): --check read-only mode with exit-code contract

**Evidence:**
- internal/cli/self_update.go (runCheck exit-code mapping)
- pkg/exitcode/exitcode.go (NotFound=3, distinct from 0 and 10)
- internal/cli/self_update_test.go TestSelfUpdate_CheckExitCodeContract

## AC: network-failure-is-safe

**Verdict:** pass

**Justification:** A release-lookup or download failure prints a clear error and returns a non-zero `exitcode.Error` before the swap (`ReplaceExecutable` runs only after a successful download+verify), so the binary is never modified; the test asserts a non-zero exit and no success line.

**Commits:**
- 35ef702 — feat(cli/self-update): network and permission failure handling; plan complete

**Evidence:**
- internal/cli/self_update.go (resolveLatest→NotFoundErrorf; doSelfReplace download err→classifySelfReplaceError)
- internal/cli/self_update_test.go TestSelfUpdate_NetworkFailureIsSafe

## AC: permission-denied-is-safe

**Verdict:** pass

**Justification:** `classifySelfReplaceError` detects `fs.ErrPermission` via `errors.Is`, prints a message including the executable path and a remedy (sudo / package manager), and returns a non-zero `exitcode.Error`; the original binary is untouched (the swap failed). Test passes.

**Commits:**
- 35ef702 — feat(cli/self-update): network and permission failure handling; plan complete

**Evidence:**
- internal/cli/self_update.go (classifySelfReplaceError)
- internal/cli/self_update_test.go TestSelfUpdate_PermissionDeniedIsSafe
